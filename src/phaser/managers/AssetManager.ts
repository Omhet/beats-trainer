import { Scene } from "phaser";

export class AssetManager {
    private scene: Scene;

    constructor(scene: Scene) {
        this.scene = scene;
    }

    createAllGraphics(): void {}
}
